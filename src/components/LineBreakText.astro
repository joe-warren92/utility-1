---
export interface Props {
  text: string;
}

const { text } = Astro.props;
const lines = text.split(/<br\s*\/?>/i);
---

{lines.map((line, index) => (
  <>
    {index > 0 && <br />}
    {line}
  </>
))}
